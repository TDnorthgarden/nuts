// Package config provides configuration management for the NUTS framework.
//
// Configuration is loaded via TOMLConfigManager (toml.go), and the main
// application bootstrap in pkg/core uses its own Config struct and
// DefaultConfig() in pkg/core/core.go.
package config
